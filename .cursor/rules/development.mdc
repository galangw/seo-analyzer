---
description:
globs:
alwaysApply: false
---
# Development Setup

The SEO Analyzer application is built with Laravel 12 and requires PHP 8.2 or higher.

## Setup Instructions

1. Clone the repository
2. Copy `.env.example` to `.env` and configure database settings
3. Run `composer install` to install PHP dependencies
4. Run `npm install` to install JavaScript dependencies
5. Run `php artisan key:generate` to generate application key
6. Run `php artisan migrate` to create database tables
7. Run `php artisan serve` to start the development server
8. Run `npm run dev` to compile frontend assets

## Development Commands

- `php artisan serve` - Start development server
- `npm run dev` - Compile assets in development mode
- `php artisan migrate:fresh --seed` - Reset database with seed data
- `php artisan test` - Run the test suite
- `composer dev` - Run all development servers concurrently
